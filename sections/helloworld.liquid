{% style %}
    .hello-world {
        background-color: {{ section.settings.bg_color }};
    }
{% endstyle %}

<h1 class="hello-world">{{ section.settings.title }}</h1>

{% schema %}
    {
        "name": "hello world",
        "settings": [
        {
            "type": "textarea",
            "id": "title",
            "label": "title",
            "default": "hellow world",
       
        },
        {
            "type": "color",
            "id": "bg_color",
            "label": "background color",
            "default": "#ffffff"
        }
        ],
        "presets": [
            {
                "name": "hello world",
                
            }
        ]
    }
{% endschema %}
 